# Orca v0.1.6

DeepSeek V4 context and CI stability release.

## Changes

- Keep DeepSeek V4 Flash and Pro context budgeting aligned to the 1M-token model window.
- Keep context-window documentation and install examples aligned with the new patch version.
- Make the grep tool contract test independent of repository README match ordering.

## Install

```bash
npm install -g @blade-ai/orca
orca --version
```

Direct binary install:

```bash
curl -fsSL https://raw.githubusercontent.com/echoVic/blade-deepseek/main/install.sh | sh
```
