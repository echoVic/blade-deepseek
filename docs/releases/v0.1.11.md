# Orca v0.1.11

Production-roadmap baseline release.

## Changes

- Refresh the production roadmap against the current Orca codebase instead of the older MVP-era status table.
- Define the next patch sequence: P1 context reliability, P2 user/workflow ergonomics, and the first skills system release.
- Record current gaps against Codex CLI and Claude Code with explicit scope boundaries for each patch release.
- Align README, website metadata, and tool comparison docs with the `v0.1.11` baseline.

## Install

```bash
npm install -g @blade-ai/orca
orca --version
```

Direct binary install:

```bash
curl -fsSL https://orcaagent.dev/install.sh | sh
```
