# Orca v0.2.0

Feature release: clarify TUI permission approval dialogs with risk-specific
titles.

## Changes

- **Permission-specific approval titles**: runtime pending interaction records
  now preserve whether a permission request is for a network block, filesystem
  write grant, or unsandboxed shell retry.
- **TUI projection**: the runtime interaction adapter projects typed permission
  requests into a dedicated TUI approval event instead of flattening them into a
  generic approval prompt.
- **Approval modal readability**: the approval dialog now shows `Network
  Permission Required`, `Filesystem Permission Required`, or `Unsandboxed Shell
  Required` when the runtime can classify the requested permission. Normal tool
  approvals still show `Approval Required`.

## Compatibility

No changes to CLI flags, slash commands, model/provider configuration, MCP
configuration, hook schema, workflow scripts, npm package layout, install
commands, server-mode wire events, JSONL event names, or tool behavior.

This release improves the TUI user experience for permission prompts while
preserving the existing approve/deny flow and runtime permission semantics.

## Verification

TDD red/green evidence:

```bash
CARGO_TARGET_DIR=/tmp/blade-deepseek-target cargo test -p orca-tui --lib tui_permission_handler_projects_runtime_permission_kind
```

Focused validation:

```bash
CARGO_TARGET_DIR=/tmp/blade-deepseek-target cargo test -p orca-runtime --lib pending_interaction_record_describes_permission_request_display
CARGO_TARGET_DIR=/tmp/blade-deepseek-target cargo test -p orca-tui --lib tui_permission_handler_projects_runtime_permission_kind
CARGO_TARGET_DIR=/tmp/blade-deepseek-target cargo test -p orca-tui --lib waiting_permission_approval_renders_specific_risk_title
CARGO_TARGET_DIR=/tmp/blade-deepseek-target cargo test -p orca-tui --lib tui_permission_handler_tracks_runtime_pending_interaction_until_resolved
CARGO_TARGET_DIR=/tmp/blade-deepseek-target cargo test -p orca-tui --lib waiting_approval_renders_numeric_shortcuts_in_semantic_order
```

Release validation before tagging:

```bash
cargo fmt -- --check
git diff --check
CARGO_TARGET_DIR=/tmp/blade-deepseek-target cargo test -p orca-runtime --lib
CARGO_TARGET_DIR=/tmp/blade-deepseek-target cargo test -p orca-tui --lib
npm --prefix site run build
npm --prefix site run check:seo
node scripts/release/test-stage-npm.mjs
node scripts/release/test-verify-published.mjs
```

Post-publish verification:

```bash
node scripts/release/verify-published.mjs --version 0.2.0 --repo echoVic/blade-deepseek --package @blade-ai/orca --bin orca
```

## Upgrade

Install or pin the release with:

```bash
npm install -g @blade-ai/orca@0.2.0
```

or:

```bash
curl -fsSL https://orcaagent.dev/install.sh | \
  INSTALL_DIR=/usr/local/bin ORCA_VERSION=0.2.0 sh
```
