# Orca v0.1.143

Patch release: isolate child-agent behavior tests so the `agent_child` module
stays a small compatibility facade while the focused child-agent runtime modules
keep owning behavior.

## Changes

- **Focused behavior-test module**: `child_agent_tests.rs` now owns the
  child-agent behavior suite that previously lived inside `agent_child.rs`.
- **Smaller facade**: `agent_child.rs` now keeps public re-exports and wrapper
  entry points without carrying the child-agent test body.
- **Ownership coverage**: a new architecture test locks the test ownership
  boundary so `agent_child.rs` does not silently become a thousand-line facade
  again.

## Compatibility

No changes to CLI flags, JSONL event names, subagent tool schema, workflow child
agent calls, async subagent workers, TUI subagent execution, model override
behavior, provider turn dispatch, provider hook execution, child compaction,
prompt-too-long recovery, response folding, tool-result folding, cost tracking,
approval policy semantics, MCP routing, external tool routing, shell session
protocol, command/exec wire shape, conversation storage, history storage, npm
package layout, or install commands.

This release is an internal ownership slice. Child-agent behavior for subagent
tool calls, workflow child agents, async subagents, TUI child-agent delegation,
provider bootstrap, model routing, provider turns, tool-call extraction,
tool-result folding, nested child-cost tracking, and turn-budget exhaustion is
unchanged; the `agent_child` compatibility facade is now small enough to keep
the next Codex/package-3-inspired runtime split easy to review.

## Verification

```bash
cargo fmt -- --check
git diff --check
CARGO_TARGET_DIR=/tmp/blade-deepseek-target-red-143 cargo test -p orca-runtime child_agent_behavior_tests_are_owned_by_focused_module -- --nocapture --test-threads=1
CARGO_TARGET_DIR=/tmp/blade-deepseek-target-143 cargo test -p orca-runtime child_agent_behavior_tests_are_owned_by_focused_module -- --nocapture --test-threads=1
CARGO_TARGET_DIR=/tmp/blade-deepseek-target-143 cargo test -p orca-runtime child_agent_tests -- --nocapture --test-threads=1
CARGO_TARGET_DIR=/tmp/blade-deepseek-target-143 cargo test -p orca-runtime child_agent -- --nocapture --test-threads=1
CARGO_TARGET_DIR=/tmp/blade-deepseek-target-143 cargo test --test subagent_contract subagent -- --nocapture --test-threads=1
CARGO_TARGET_DIR=/tmp/blade-deepseek-target-143 cargo test -p orca-tui tui_subagent_child -- --nocapture --test-threads=1
CARGO_TARGET_DIR=/tmp/blade-deepseek-target-143 cargo clippy -p orca-runtime --lib --tests
npm --prefix site run build
npm --prefix site run check:seo
node scripts/release/test-stage-npm.mjs
node scripts/release/test-verify-published.mjs
```

Before publishing, run the full release gate:

```bash
CARGO_TARGET_DIR=/tmp/blade-deepseek-target-143 cargo test -- --test-threads=1
CARGO_TARGET_DIR=/tmp/blade-deepseek-target-143 cargo clippy --workspace --all-targets
node scripts/release/real-api-e2e.mjs --skip-build --orca-bin /tmp/blade-deepseek-target-143/debug/orca --timeout-ms 180000
```

After publishing, verify the public artifacts:

```bash
node scripts/release/verify-published.mjs --version 0.1.143 --repo echoVic/blade-deepseek --package @blade-ai/orca --bin orca
```

## Install

```bash
npm install -g @blade-ai/orca
orca --version
```

Direct binary install:

```bash
curl -fsSL https://orcaagent.dev/install.sh | sh
```

Pin a specific version:

```bash
curl -fsSL https://orcaagent.dev/install.sh | \
  INSTALL_DIR=/usr/local/bin ORCA_VERSION=0.1.143 sh
```
