# Orca v0.1.20

Documentation and website refresh.

## Changes

- Update README tool documentation to include the interactive `request_user_input` TUI path.
- Refresh the tools comparison document from the older v0.1.11 baseline to the current v0.1.20 capabilities.
- Update website copy and metadata so the public page mentions Markdown skills and TUI structured user input.
- Keep release metadata aligned across Cargo, README, roadmap, site metadata, and release notes.

## Install

```bash
npm install -g @blade-ai/orca
orca --version
```

Direct binary install:

```bash
curl -fsSL https://orcaagent.dev/install.sh | sh
```
